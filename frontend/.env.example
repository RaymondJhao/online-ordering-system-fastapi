# 複製本檔為 .env.local 後調整。Vite 只會把 VITE_ 開頭的變數注入前端。
#
# 注意：這些值會被打包進 JavaScript，任何人都看得到，
# 因此**絕對不要放任何金鑰或密碼**。

# 後端 API 位址。
#
# 本機開發請留空——留空時所有請求走相對路徑 /api/*，
# 由 vite.config.js 的 dev proxy 轉發到 http://localhost:8000，
# 這樣就不會有跨網域問題。
#
# 部署到 Vercel 時必須填入後端網址，例如：
#   VITE_API_BASE_URL=https://ordering-backend.onrender.com
# 沒填的話 /api/orders 會打到 Vercel 自己的網域而得到 404。
VITE_API_BASE_URL=
